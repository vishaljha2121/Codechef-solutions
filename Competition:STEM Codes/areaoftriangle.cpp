#include <bits/stdc++.h>

using namespace std;
int main()
{
    int height = 10, base = 6;
    /* cin >> height >> base; */
    float area;
    area = (0.5) * base * height;
    cout << area << endl;
}